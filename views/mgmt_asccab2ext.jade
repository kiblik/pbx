extends mgmt

block mgmt_content
  table(table)
    tr
      td
        select(class="form-control",id='new_ID_Ext')
          each row in rows_exts
            option(value=row.ID)
              =row.Ext + " (" + row.Name + ")"   
      td
        select(class="form-control",id='new_ID_aScCab')
          each row in rows_asccab
            option(value=row.ID)
              =row.name
      td
        button(class="btn btn-primary",onClick="add('aScCab2Ext');return false;")
          | Add


  p(id='chart')
  script(src='/javascripts/asccab2ext.js')
      
block additionalScripts
  include sankey